sudo: required

language: java

services:
  - docker

jdk:
  - oraclejdk8
before_install:
  - chmod +x gradlew
  - chmod +x gradle/wrapper/gradle-wrapper.jar
  - ./gradlew war

script:
  - docker build -t bonte/kickoffkata .
